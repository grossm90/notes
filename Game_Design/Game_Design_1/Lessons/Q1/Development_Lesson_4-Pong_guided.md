
If we want to do math with a variable and update it at the same time, like x = x + 1, we can shorten that to BLANK

A simple way to check collision with rectangle hit-boxes is AABB collision which is called BLANK-BLANK BLANK-BLANK collision.