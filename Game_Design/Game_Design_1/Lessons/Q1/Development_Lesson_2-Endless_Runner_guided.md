
BLANK is text written in the code that does not affect the code, it's just notes for the developers.

A line is made up of BLANK points, a point is made up of BLANK coordinates.

The `line()` function needs BLANK coordinates to draw a line.

On a vertical line, the BLANK coordinates do not change.

On a horizontal line, the BLANK coordinates do not change.

If you want to check if two values are equal, you must use BLANK equal signs

In PICO-8, objects are drawn in BLANK, meaning objects drawn to the screen first are drawn BLANK objects drawn after.