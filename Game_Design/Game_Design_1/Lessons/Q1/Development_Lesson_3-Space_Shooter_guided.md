
2d images in video games are called BLANKs

In PICO-8, a sprite's (𝑥, 𝑦) is its BLANK-BLANK corner

A custom block of code with its own name is called a BLANK

A BLANK is a value that is either true or false

In PICO-8, a collection of variables is called a BLANK, in other languages this is called a list or an array

We can run code multiple times without having to re-write it by using a BLANK