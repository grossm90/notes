
A BLANK is how we used to interact with our computers before graphic interfaces. It is also sometimes referred to as a command line interface or C.L.I. for short.

Typing BLANK into the terminal and pressing `ENTER` brings up the help menu.

BLANK is an old computer term for a folder.

To create a folder called `lessons` in PICO-8 we type `BLANK lessons` and press `ENTER`.

To show all of the files and folders in our current folder, we type `BLANK` and press `ENTER`.

To move into the `lessons` folder, we type `BLANK lessons` and press `ENTER`.

To show text on the screen we use the `BLANK()` function. For example `BLANK("hello, world!")`. The text we want to show *must* be in quotes.

To learn more about the `cls()` function, we type `help BLANK`.

In game engines, the origin (0, 0) is on the BLANK BLANK corner of the screen *not* the center.

In game engines, y gets BLANK the lower you go on the screen.

The PICO-8 screen is a square, and is BLANK pixels tall and wide. Screens these days are rectangles.